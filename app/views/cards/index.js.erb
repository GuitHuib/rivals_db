// remove list var? use refactored out?
var list = $("#card-list")

// empty card list and append search results
var results = $(".results")
results.empty()
<% @cards.each do |card| %>
  results.append("<%= escape_javascript(render 'card', card: card) %>")
<% end %>
document.getElementById("search-form").reset()
