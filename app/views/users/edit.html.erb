<% provide(:title, 'Eidt profile') %>
<h1>Edit profile</h1>

<%= render 'form' %>

<%= link_to "Delete Profile", @user, method: :delete,
                                       data: { confirm: "You sure? This will permanently delete your profile." } %>
